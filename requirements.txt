# ─── ROBI — Python bağımlılıkları ────────────────────────────────────────────
# pip install -r requirements.txt

# Temel
openai>=1.30.0
python-dotenv>=1.0.0
numpy>=1.24.0

# Ses
webrtcvad>=2.0.10
vosk>=0.3.45
sounddevice>=0.4.6      # isteğe bağlı (arecord kullanılıyor)

# Görüntü (sistem paketleri tercih edilir)
# opencv-python           # Pi'da: sudo apt install python3-opencv
# picamera2               # Pi'da: sudo apt install python3-picamera2
# face_recognition        # Pi'da: pip install face_recognition (dlib derleme ~90 dk)

# Donanım
RPi.GPIO>=0.7.0           # Pi'da genellikle kurulu

# ─── Kurulum notları ─────────────────────────────────────────────────────────
# Sistem paketleri (Pi'da):
#   sudo apt update
#   sudo apt install -y python3-opencv python3-picamera2 python3-numpy
#   sudo apt install -y mpv espeak-ng yt-dlp
#
# face_recognition (dlib derleme gerektirir, ~60-90 dakika):
#   pip install face_recognition
#
# venv için sistem paketlerine erişim:
#   python3 -m venv venv --system-site-packages
